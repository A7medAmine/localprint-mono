// TRANSLATIONS + ALLOWED_TYPES now live in @localprint/shared/constants (the
// online app uses a subset of the keys). Re-exported here so the app's
// "./constants" importers stay untouched.
export * from "@localprint/shared/constants";
