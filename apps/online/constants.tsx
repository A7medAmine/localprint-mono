// TRANSLATIONS + ALLOWED_TYPES now live in @atba3li/shared/constants (the
// online app uses a subset of the keys). Re-exported here so the app's
// "./constants" importers stay untouched.
export * from "@atba3li/shared/constants";
